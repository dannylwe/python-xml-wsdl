### To run project

##### Install pipenv
`pip install pipenv`

#### Install packages
`run` pipenv shell  
`run` pipenv install

#### Run script
`python calcaulator.py`

#### Services schema
`python -mzeep http://www.dneonline.com/calculator.asmx?WSDL`